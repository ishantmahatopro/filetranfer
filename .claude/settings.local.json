{
  "permissions": {
    "allow": [
      "Bash(git add *)",
      "Bash(git -c user.name=Claude -c user.email=noreply@anthropic.com commit -m 'Add consent-based location-sharing page with VPN/geofence gating *)",
      "Bash(git push *)"
    ]
  }
}
